# The deployed HTTP API's invoke URL (API Gateway), no trailing slash.
# e.g. https://abc123.execute-api.us-east-1.amazonaws.com
VITE_API_URL=

# The deployed WebSocket API's invoke URL, no trailing slash.
# e.g. wss://xyz789.execute-api.us-east-1.amazonaws.com/prod
VITE_WS_URL=
